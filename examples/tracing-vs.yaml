apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: jaeger
spec:
  hosts:
  - "jaeger.iop11.com"
  gateways:
  - iop-gateway
  http:
  - match:
    - uri:
        prefix: /
    rewrite:
      uri: /
    route:
    - destination:
        host: tracing.istio-system.svc.cluster.local
        port:
          number: 80
